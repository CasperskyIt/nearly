// Stub spec — source file created in Plan 01
// Imports added by the Plan 01 executor once auth.guard.ts exists

describe('authGuard', () => {
  xit('should allow navigation when currentUser is not null', () => { pending(); });
  xit('should redirect to /:app/login when currentUser is null', () => { pending(); });
  xit('should include returnUrl as a query param in the redirect', () => { pending(); });
});

describe('loginRedirectGuard', () => {
  xit('should allow navigation when currentUser is null', () => { pending(); });
  xit('should redirect to /:app when currentUser is already authenticated', () => { pending(); });
});
