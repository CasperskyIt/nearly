---
phase: 02-authentication
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/app/services/supabase.service.spec.ts
  - frontend/src/app/guards/auth.guard.spec.ts
  - frontend/src/app/pages/login/login.component.spec.ts
  - frontend/src/app/pages/auth-callback/auth-callback.component.spec.ts
  - frontend/src/app/components/header/header.component.spec.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Every component and service created in Phase 2 has a corresponding spec file before implementation begins"
    - "Running the full test suite does not fail due to missing spec files"
    - "Each spec file contains at least one pending or passing describe block"
  artifacts:
    - path: "frontend/src/app/services/supabase.service.spec.ts"
      provides: "Spec scaffold for auth state signal, signInWithOAuth, signOut"
      contains: "describe('SupabaseService'"
    - path: "frontend/src/app/guards/auth.guard.spec.ts"
      provides: "Spec scaffold for authGuard and loginRedirectGuard"
      contains: "describe('authGuard'"
    - path: "frontend/src/app/pages/login/login.component.spec.ts"
      provides: "Spec scaffold for LoginComponent"
      contains: "describe('LoginComponent'"
    - path: "frontend/src/app/pages/auth-callback/auth-callback.component.spec.ts"
      provides: "Spec scaffold for AuthCallbackComponent"
      contains: "describe('AuthCallbackComponent'"
    - path: "frontend/src/app/components/header/header.component.spec.ts"
      provides: "Spec scaffold for HeaderComponent auth UI"
      contains: "describe('HeaderComponent'"
  key_links: []
---

<objective>
Create skeleton spec files for all components, services, and guards that Phase 2 will create or modify. These stubs satisfy the Nyquist Wave 0 requirement so that automated verification commands in Plans 01-03 have spec files to target.

Purpose: Ensures the test infrastructure is in place before implementation begins. Each spec contains a `describe` block with `pending()` or `xit()` placeholders that document expected behaviors — executors in later plans fill these in as they implement.
Output: 5 spec files with skeleton test scaffolds.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-VALIDATION.md

@frontend/src/app/services/supabase.service.ts
@frontend/src/app/components/header/header.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton spec files for auth service, guards, and page components</name>
  <files>
    frontend/src/app/services/supabase.service.spec.ts,
    frontend/src/app/guards/auth.guard.spec.ts,
    frontend/src/app/pages/login/login.component.spec.ts,
    frontend/src/app/pages/auth-callback/auth-callback.component.spec.ts,
    frontend/src/app/components/header/header.component.spec.ts
  </files>
  <read_first>
    frontend/src/app/services/supabase.service.ts,
    frontend/src/app/components/header/header.component.ts,
    frontend/tsconfig.spec.json,
    frontend/karma.conf.js
  </read_first>
  <action>
    Create 5 spec files. Each file should:
    - Import the target class/function (use the path that will exist after Plan 01 creates it — for files that do not exist yet like auth.guard.ts, login.component.ts, auth-callback.component.ts, use `xit` or `pending()` so the spec compiles even if the import target is missing)
    - Contain a top-level `describe()` block
    - Contain `xit()` placeholder tests documenting expected behaviors (NOT `it()` — these are stubs, not real tests)

    **Important:** Because Plans 01 and 02 have not executed yet, the guard file and page component files do not exist. For those specs, do NOT import the missing modules at the top level. Instead, wrap the `describe` block body in placeholder tests using `xit()` so Karma does not fail on missing imports.

    Pattern for spec files where the source file DOES exist (supabase.service.spec.ts, header.component.spec.ts):
    ```typescript
    import { TestBed } from '@angular/core/testing';
    import { SupabaseService } from './supabase.service';

    describe('SupabaseService', () => {
      xit('should expose a currentUser signal initialized to null', () => { pending(); });
      xit('should populate currentUser after initSession()', () => { pending(); });
      xit('should call signInWithOAuth with google provider and PKCE flow', () => { pending(); });
      xit('should call supabase.auth.signOut on signOut()', () => { pending(); });
      xit('should update currentUser on onAuthStateChange events', () => { pending(); });
    });
    ```

    Pattern for spec files where the source file DOES NOT yet exist (auth.guard.spec.ts, login.component.spec.ts, auth-callback.component.spec.ts):
    ```typescript
    // Stub spec — source file created in Plan 01/02
    describe('authGuard', () => {
      xit('should allow navigation when currentUser is not null', () => { pending(); });
      // ... more xit stubs
    });
    ```
    Do NOT add imports for files that do not exist yet. The executor in Plans 01-03 will update these specs to add real imports and real test bodies.

    **supabase.service.spec.ts** stubs (AUTH-01, AUTH-02):
    - currentUser signal initialized to null
    - initSession populates currentUser from getUser()
    - signInWithGoogle calls signInWithOAuth with provider 'google'
    - signOut calls supabase.auth.signOut
    - onAuthStateChange updates currentUser signal

    **auth.guard.spec.ts** stubs (AUTH-02, AUTH-03):
    - authGuard allows navigation when user is authenticated
    - authGuard redirects to /:app/login when user is null
    - authGuard includes returnUrl in redirect
    - loginRedirectGuard allows navigation when user is null
    - loginRedirectGuard redirects to /:app when user is authenticated

    **login.component.spec.ts** stubs (AUTH-01):
    - renders Sign in with Google button
    - disables button and shows spinner during loading
    - displays error message when errorMessage signal is set
    - calls signInWithGoogle on button click
    - displays OAuth cancellation message from query param (per D-11)

    **auth-callback.component.spec.ts** stubs (AUTH-03):
    - subscribes to onAuthStateChange on init
    - navigates to returnUrl on SIGNED_IN event
    - navigates to /login on INITIAL_SESSION without session
    - unsubscribes from auth listener after navigation
    - checks for error query param and redirects to login with errorMessage (per D-11)

    **header.component.spec.ts** stubs (AUTH-04):
    - shows Sign in link when currentUser is null
    - shows avatar circle when currentUser is present
    - opens MatMenu on avatar click
    - opens sign-out confirmation dialog
    - navigates to login after confirmed sign-out
  </action>
  <verify>
    <automated>cd /Users/karzy/Private/Repositories/nearly/frontend && npx tsc --noEmit 2>&1 | head -20 && npm test -- --watch=false --browsers=ChromeHeadless 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 spec files exist with xit() placeholder tests. Test suite runs without failures (all placeholders are pending/excluded). Wave 0 requirement from VALIDATION.md is satisfied.</done>
</task>

</tasks>

<verification>
1. All 5 spec files exist: `ls frontend/src/app/services/supabase.service.spec.ts frontend/src/app/guards/auth.guard.spec.ts frontend/src/app/pages/login/login.component.spec.ts frontend/src/app/pages/auth-callback/auth-callback.component.spec.ts frontend/src/app/components/header/header.component.spec.ts`
2. `cd frontend && npm test -- --watch=false --browsers=ChromeHeadless` passes (all specs are pending/excluded, no failures)
3. Each file contains a `describe()` block: `grep -l 'describe(' frontend/src/app/{services/supabase.service,guards/auth.guard,pages/login/login.component,pages/auth-callback/auth-callback.component,components/header/header.component}.spec.ts`
</verification>

<success_criteria>
- 5 spec files created at the paths listed in VALIDATION.md Wave 0 Requirements
- Test suite runs green (all placeholder tests are xit/pending, not failing)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-00-SUMMARY.md`
</output>
