---
phase: 03-dog-profiles
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - frontend/src/app/components/header/header.component.ts
  - frontend/src/app/components/header/header.component.html
  - frontend/src/app/components/header/header.component.scss
autonomous: false
requirements: [DOG-05]

must_haves:
  truths:
    - "Header displays a dog selector dropdown when user is authenticated and has dogs"
    - "Dog selector shows current dog name with avatar and dropdown arrow"
    - "Selecting a different dog updates DogService.currentDog signal"
    - "Dog selector is hidden when user has no dogs or is not authenticated"
    - "All dog pages are navigable: list, create, detail with tabs, edit modal, delete"
  artifacts:
    - path: "frontend/src/app/components/header/header.component.ts"
      provides: "Updated header with dog selector"
      contains: "DogService"
    - path: "frontend/src/app/components/header/header.component.html"
      provides: "Dog selector dropdown in header"
      contains: "currentDog"
  key_links:
    - from: "frontend/src/app/components/header/header.component.ts"
      to: "frontend/src/app/services/dog.service.ts"
      via: "inject(DogService) for currentDog signal and dogs signal"
      pattern: "inject\\(DogService\\)"
    - from: "frontend/src/app/components/header/header.component.html"
      to: "frontend/src/app/services/dog.service.ts"
      via: "dogService.currentDog() and dogService.dogs() in template"
      pattern: "dogService\\.currentDog|dogService\\.dogs"
---

<objective>
Add a dog selector dropdown to the HeaderComponent and perform a final integration verification across all dog profile features. The header dropdown allows switching between dogs and shows the current dog's name and avatar.

Purpose: Deliver the persistent current-dog state indicator in the header (D-03) and ensure all Phase 3 features work end-to-end. This is the final plan that ties together database, service, and all UI components.
Output: Updated HeaderComponent with dog selector; full Phase 3 integration verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dog-profiles/03-CONTEXT.md
@.planning/phases/03-dog-profiles/03-02-SUMMARY.md
@.planning/phases/03-dog-profiles/03-03-SUMMARY.md
@.planning/phases/03-dog-profiles/03-04-SUMMARY.md
@frontend/src/app/components/header/header.component.ts
@frontend/src/app/components/header/header.component.html
@frontend/src/app/components/header/header.component.scss
@frontend/src/app/services/dog.service.ts

<interfaces>
From frontend/src/app/components/header/header.component.ts:
```typescript
@Component({
  selector: 'app-header',
  standalone: true,
  imports: [MatToolbarModule, MatIconModule, MatButtonModule, MatMenuModule, MatDialogModule, RouterLink],
  templateUrl: './header.component.html',
  styleUrl: './header.component.scss'
})
export class HeaderComponent {
  menuClick = output<void>();
  protected supabase = inject(SupabaseService);
  private router = inject(Router);
  private dialog = inject(MatDialog);
  protected themeService = inject(ThemeService);
  // ... methods: getInitials, getAppPrefix, onSignIn, onSignOut, onMyAccount
}
```

From frontend/src/app/components/header/header.component.html:
```html
<mat-toolbar color="primary">
  <span>{{ themeService.theme?.strings?.appName ?? 'Nearly' }}</span>
  <span class="spacer"></span>
  @if (supabase.currentUser() === null) {
    <button mat-button class="sign-in-link" (click)="onSignIn()">Sign in</button>
  } @else {
    <!-- avatar button with user menu -->
  }
</mat-toolbar>
```

From frontend/src/app/services/dog.service.ts:
```typescript
export class DogService {
  currentDog = signal<Dog | null>(null);
  dogs = signal<Dog[]>([]);
  setCurrentDog(dog: Dog | null): void;
  async restoreCurrentDog(): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dog selector dropdown to HeaderComponent</name>
  <files>
    frontend/src/app/components/header/header.component.ts
    frontend/src/app/components/header/header.component.html
    frontend/src/app/components/header/header.component.scss
  </files>
  <read_first>
    - frontend/src/app/components/header/header.component.ts (current implementation)
    - frontend/src/app/components/header/header.component.html (current template)
    - frontend/src/app/components/header/header.component.scss (current styles)
    - frontend/src/app/services/dog.service.ts (currentDog, dogs signals, setCurrentDog, restoreCurrentDog)
    - .planning/phases/03-dog-profiles/03-CONTEXT.md (D-03)
  </read_first>
  <action>
Update HeaderComponent to include a dog selector dropdown (per D-03).

**header.component.ts changes:**
1. Add import for `DogService`: `import { DogService } from '../../services/dog.service';`
2. Add import for `Dog`: `import { Dog } from '../../models/dog.model';`
3. Add injection: `protected dogService = inject(DogService);`
4. Add method `onSelectDog(dog: Dog): void` that calls `this.dogService.setCurrentDog(dog)` and navigates to `/${this.getAppPrefix()}/dogs/${dog.id}`
5. Add method `onMyDogs(): void` that navigates to `/${this.getAppPrefix()}/dogs`

**header.component.html changes:**
Add the dog selector between the app name and the spacer, ONLY when user is authenticated AND has dogs. Insert this block after the app name `<span>` and before `<span class="spacer">`:

```html
<!-- Dog selector (per D-03) -->
@if (supabase.currentUser() && dogService.dogs().length > 0) {
  <button mat-button [matMenuTriggerFor]="dogMenu" class="dog-selector">
    <div class="dog-selector-content">
      @if (dogService.currentDog()?.avatar_url) {
        <img [src]="dogService.currentDog()!.avatar_url" class="dog-selector-avatar" alt="" />
      } @else {
        <mat-icon class="dog-selector-icon">pets</mat-icon>
      }
      <span class="dog-selector-name">{{ dogService.currentDog()?.name ?? 'Select Dog' }}</span>
      <mat-icon>arrow_drop_down</mat-icon>
    </div>
  </button>
  <mat-menu #dogMenu="matMenu">
    @for (dog of dogService.dogs(); track dog.id) {
      <button mat-menu-item (click)="onSelectDog(dog)">
        @if (dog.avatar_url) {
          <img [src]="dog.avatar_url" class="menu-dog-avatar" alt="" />
        } @else {
          <mat-icon>pets</mat-icon>
        }
        <span>{{ dog.name }}</span>
      </button>
    }
    <mat-divider></mat-divider>
    <button mat-menu-item (click)="onMyDogs()">
      <mat-icon>list</mat-icon>
      <span>All Dogs</span>
    </button>
  </mat-menu>
}
```

Also add a "My Dogs" menu item to the existing user menu (the avatar dropdown), between "My Account" and "Sign out":
```html
<button mat-menu-item (click)="onMyDogs()">
  <mat-icon>pets</mat-icon>
  <span>My Dogs</span>
</button>
```

**header.component.ts imports update:**
Add `MatDividerModule` to the imports array: `imports: [MatToolbarModule, MatIconModule, MatButtonModule, MatMenuModule, MatDialogModule, MatDividerModule, RouterLink]`

**header.component.scss additions:**
```scss
.dog-selector {
  margin-left: 12px;
}

.dog-selector-content {
  display: flex;
  align-items: center;
  gap: 6px;
}

.dog-selector-avatar {
  width: 24px;
  height: 24px;
  border-radius: 50%;
  object-fit: cover;
}

.dog-selector-icon {
  font-size: 20px;
}

.dog-selector-name {
  max-width: 100px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.menu-dog-avatar {
  width: 24px;
  height: 24px;
  border-radius: 50%;
  object-fit: cover;
  margin-right: 8px;
}
```
  </action>
  <acceptance_criteria>
    - `header.component.ts` contains `inject(DogService)`
    - `header.component.ts` contains `import { DogService }` from dog.service
    - `header.component.ts` contains `onSelectDog` method
    - `header.component.ts` contains `onMyDogs` method
    - `header.component.ts` imports include `MatDividerModule`
    - `header.component.html` contains `dogMenu` (mat-menu reference)
    - `header.component.html` contains `dogService.currentDog()`
    - `header.component.html` contains `dogService.dogs()`
    - `header.component.html` contains `arrow_drop_down` icon
    - `header.component.html` contains "All Dogs" menu item
    - `header.component.html` contains "My Dogs" in user menu
    - `header.component.scss` contains `.dog-selector`
    - `header.component.scss` contains `.dog-selector-avatar`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "inject(DogService)" frontend/src/app/components/header/header.component.ts && grep -q "dogService.currentDog()" frontend/src/app/components/header/header.component.html && grep -q "dogService.dogs()" frontend/src/app/components/header/header.component.html && grep -q "dogMenu" frontend/src/app/components/header/header.component.html && grep -q "My Dogs" frontend/src/app/components/header/header.component.html && grep -q "dog-selector" frontend/src/app/components/header/header.component.scss && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>HeaderComponent displays dog selector dropdown when user is authenticated and has dogs; shows current dog avatar + name + dropdown arrow; selecting a dog updates currentDog signal and navigates to detail page; "My Dogs" link added to user menu; "All Dogs" link in dog selector menu</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Phase 3 dog profile flow</name>
  <files>none</files>
  <action>
    Human verification of the complete Phase 3 dog profiles feature. No code changes — this is a manual walkthrough of the full flow to confirm all features work end-to-end before marking the phase complete.
  </action>
  <what-built>
    Complete dog profiles feature: database schema with RLS, DogService with CRUD + avatar upload, dog list page, dog create page, dog detail hub with tabs (Profile, Care, Health, Co-Guardians), edit modal with avatar upload, delete with confirmation, and header dog selector dropdown.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm start`
    2. Navigate to `http://localhost:4200/dogly/dogs` — should redirect to login if not authenticated
    3. Sign in with Google
    4. After sign-in, navigate to `/dogly/dogs` — should see empty state with "No dogs yet" message and "Add Dog" button
    5. Click the "+" icon or "Add Dog" button — should navigate to `/dogly/dogs/new`
    6. Fill in only the name field (e.g., "Buddy") and click "Add Dog" — should create the dog and navigate to its detail page
    7. Verify the detail page shows 4 tabs: Profile, Care, Health, Co-Guardians
    8. On Profile tab: verify dog name shows, edit and delete buttons are visible
    9. Click "Edit" — modal should open with name pre-filled
    10. Add a breed (e.g., "Golden Retriever"), pick a date of birth, click "Save" — detail page should update without reload
    11. Try uploading an avatar via the edit modal — click the avatar area, select an image, verify preview shows, click Save
    12. Verify avatar appears on detail page and in the dog list
    13. Navigate to `/dogly/dogs` — verify dog appears in list with avatar (or fallback icon)
    14. Check the header — if you have at least one dog, the dog selector dropdown should appear
    15. Click "Care" tab — should show "No care entries yet. Care logging ships in Phase 5."
    16. Click "Health" tab — should show "No health records yet. Health records ship in Phase 6."
    17. Create a second dog to test the dog selector dropdown switching
    18. Test delete: on a dog's detail page, click Delete, confirm — should redirect to dog list and dog should be gone
    19. Verify RLS: the dog list should only show dogs you own (you cannot see other users' dogs)
  </how-to-verify>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification of full Phase 3 flow"</automated>
  </verify>
  <done>Human has verified all 19 test steps pass: dog CRUD works, avatar upload works, tabs display correctly, header selector works, RLS enforces access control</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Header dog selector appears when user has dogs, hidden otherwise
- Dog selector shows current dog name and avatar
- Switching dogs updates the currentDog signal
- Full flow works: list -> create -> detail -> edit -> delete
- All 4 tabs visible on detail page
- Care and Health tabs show Phase 5/6 placeholder text
- Avatar upload works end-to-end
- RLS prevents viewing other users' dogs
</verification>

<success_criteria>
- All 6 ROADMAP Success Criteria for Phase 3 are verifiable
- Header integration complete (D-03)
- Full dog CRUD lifecycle works without errors
- Human verification confirms visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/03-dog-profiles/03-05-SUMMARY.md`
</output>
